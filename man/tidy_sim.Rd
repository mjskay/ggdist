% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_link_sim.R
\name{tidy_sim}
\alias{tidy_sim}
\title{Tidy posterior observations for map and map2stan models}
\usage{
tidy_sim(data, fit, name = NULL, ...)
}
\arguments{
\item{data}{Optional list of data to compute predictions over. When missing, uses data found inside fit object.}

\item{fit}{Object of class \code{\link[rethinking]{map}} or \code{\link[rethinking]{map2stan}}.}

\item{name}{If \code{NULL}, uses the name of the first response specification in the model with ".predicted"
appended as the column representing predictions. Otherwise, gives a bare (unquoted) name to assign
predicted observations to.}

\item{...}{Additional arguments for \code{\link[rethinking]{sim}}.

Simulates posterior observations for all link functions in \code{fit} and returns a tidy-format data frame
consisting of multiple samples for each row of \code{data}.}
}
\description{
Simulates posterior observations for \code{\link[rethinking]{map}} and \code{\link[rethinking]{map2stan}} models
in a tidy data format (a tidy version of \code{\link[rethinking]{sim}}).
}
\seealso{
\code{\link[rethinking]{sim}}
}

