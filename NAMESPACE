importFrom(plyr, ldply)
importFrom(tidyr, spread, spread_, separate)
import(reshape2)
import(lazyeval)
import(dplyr)
import(ggplot2)
import(stringi)

export(
    data_list,
    as.data_list,
    compose_data,

    apply_prototypes,
    tidy_samples,
    compare_levels,

    tidy_link,
    tidy_sim,
    
    seq_range,
    
    mean_qi,
    
    geom_eye,
    ggeye,
    mode_hdi,

    predict_curve,
    predict_curve_density,
    density_bins,
    histogram_bins
    )

S3method(c, data_list)
S3method(print, data_list)
S3method(as.data_list, default)
S3method(as.data_list, numeric)
S3method(as.data_list, logical)
S3method(as.data_list, factor)
S3method(as.data_list, list)
S3method(as.data_list, data.frame)
S3method(as.data_list, data_list)
